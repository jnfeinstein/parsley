var React = require('react/addons');

var LoadingComponent = React.createClass({
  render: function() {
    return (
      <div>
        Loading...
      </div>
    );
  }
});

module.exports = LoadingComponent;
